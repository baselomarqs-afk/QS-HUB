# 🏢 التقرير الهندسي الكامل والصريح: THE QS HUB — محرك القياس الرياضي

> تقرير مبني على فحص فعلي للكود ونتائج التحقق (validation) الخاصة بالمشروع.
> كل ادعاء فيه مرفق بموضعه في الكود ليكون قابلاً للتحقق.
> التاريخ: 2026-06-25

---

## 0) الملخص التنفيذي (Executive Summary)

الفكرة المعمارية للمنصة **صحيحة واتجاهها سليم**: نظام هجين يستخدم الذكاء الاصطناعي لِما يجيده (قراءة الأسماء والجداول)، والرياضيات لِما تجيده (القياس). لكن **التنفيذ الحالي ما زال نموذجاً أولياً (Prototype)**، وقد سُوِّق في تقرير سابق على أنه "منتج جاهز بدقة 100%" — وهذا غير صحيح ويكذّبه الكود ونتائج التحقق الخاصة بالمشروع نفسه.

**الخلاصة بكلمة:** المنصة في منتصف الطريق لا في نهايته. لا تُرفَع للإنتاج بوصف "دقة 100%" قبل إصلاح المحرك فعلياً.

---

## 1) التقييم الحقيقي (بالأرقام)

| الجانب | تقييم التقرير الدعائي | الواقع المُثبَت | الأساس |
|--------|----------------------|------------------|--------|
| دقة الكميات (QTO Accuracy) | 9.5 / 10 | **~4 / 10** | 57% من البنود خطؤها > 30% في الـ validation الخاص بالمشروع |
| جودة الهندسة البرمجية | 9 / 10 | **~6 / 10** | فكرة هجينة سليمة، لكن قياس تخميني + كود غير موصول + جذر مشروع فوضوي |
| الجاهزية للبيع (Production) | جاهز | **ليس بعد** | ادعاء "دقة 100%" مقابل محيط = 40% من الحقيقة = مخاطرة سمعة/قانونية |

---

## 2) الأدلة الأربعة المؤكَّدة (Confirmed Defects)

### الدليل #1 — "الدقة 100%" هي تخمين ثابت مكتوب يدوياً
الموضع: `workflow/vector_arch_measure.py:96-103`
```python
total_centerline_m = (total_length_pts * pt_to_m) / 2.0
estimated_ext_m = total_centerline_m * 0.25   # ثابت تخميني
estimated_int_m = total_centerline_m * 0.75   # ثابت تخميني
```
المحيط الخارجي = مجموع كل الخطوط ÷ 2 × **0.25**. ثم يُلصق عليه في `workflow/step3_extract.py:810` وصف `[100% Precision ... computed geometrically from CAD vectors]`. **رقم مضروب في ثابت لا يمكن أن يكون "دقة مليمتر".**

### الدليل #2 — احتساب كل الخطوط كجدران (فشل تصفية الضوضاء)
الموضع: `workflow/vector_arch_measure.py:50-69`
الفلتر الوحيد هو طول الخط بين 0.3 و 30 م. لذلك تُحسَب كجدران: خطوط الأبعاد، الأثاث، خطوط الشبكة، إطار اللوحة، التظليل، السلالم. ثم يُقسَم الناتج على 2 بافتراض أن **كل جدار = خطّان متوازيان فقط** — افتراض ينهار في أي مخطط حقيقي.

### الدليل #3 — فشل في الـ Validation Set
الموضع: `validation_set/batch_p1.log`
```
<=15%                  4   (17%)
<=30%                  2   (9%)
>30%                  13   (57%)   ← أكثر من النصف خطؤه فوق 30%
missing (engine=0)     4   (17%)
```
أمثلة فعلية من `validation_set/batch_results.json`:

| البند | المحرك | الحقيقي | الخطأ |
|------|--------|---------|-------|
| external_paint | 0 | 983 | 100% |
| external_plaster | 272.9 | 983 | 72% |
| column_concrete | 0 | 38 | 100% |
| backfill | 300 | 1465 | 79% |
| interlock_paving | 707 | 234 | 202% |

والمحيط المُقاس = **23.6 م** مقابل أبعاد مبنى 15.78 × 12.72 (محيطه الحقيقي ≈ 57 م) → المحرك أعطى **40% من المحيط الحقيقي**، وهو الأثر المباشر لمعادلة 0.25.

> **تنبيه إنصاف:** قد يكون `batch_p1.log` من تشغيل سابق لمحرّك الفيكتور الحالي. لكن قيمة المحيط (23.6 مقابل 57) هي بالضبط نمط فشل معادلة 0.25 الموجودة في الكود الآن. يجب إعادة تشغيل الـ validation على النسخة الحالية للحصول على رقم محدّث.

### الدليل #4 — كود غير موصول (Disconnected Code)
- دالتا `extract_wall_data` (`workflow/wall_extractor.py:21`) و`extract_arch_data` (`workflow/arch_extractor.py:31`) **لا تُستدعيان من أي مكان** — فقط الـ prompts النصية تُستورد في `step3_extract.py:733-734`.
- محرك الفيكتور يعمل فقط في مسار FastAPI (`api/routers/extraction.py:118`، حيث يُمرَّر `pdf_path`).
- مسار العامل الخلفي `worker.py:73` و`worker.py:142` يستدعي `extract_page(...)` **بدون `pdf_path`** → الفيكتور لا يعمل هناك إطلاقاً، يبقى تخمين Gemini الكامل.

---

## 3) خطة الإصلاح المعتمدة (Remediation Plan)

الخطة المقترحة سابقاً صحيحة في الاتجاه، لكنها أُكمِلت هنا بثلاث ثغرات حرجة كانت ناقصة.

### المرحلة A — إصلاح المقياس أولاً (الأهم، كان مفقوداً)
> أخطر مصدر خطأ على الإطلاق. لو المقياس خطأ، كل القياسات خطأ — bbox أو أزواج جدران لن تنقذ.
- `detect_page_scale` (`vector_arch_measure.py:13`) حالياً يفترض 1:100 عند عدم العثور على النص.
- **الإصلاح:** اعتماد على مرجع قياس حقيقي: قراءة طول خط أبعاد معروف من الـ OCR ومقارنته بطوله بالـ points لاستنتاج المقياس الفعلي، بدل افتراض ثابت. عند تعذّر ذلك → وضع علامة `confidence: low` صراحة، لا الادعاء بالدقة.

### المرحلة B — قياس الجدران بالطريقة الصحيحة (النقطة #2 الأصلية، صحيحة)
- إلغاء ثابتَي 0.25 / 0.75 تماماً.
- كشف **أزواج الخطوط المتوازية** المتباعدة بمسافة تطابق سماكة جدار (≈ 100mm أو 200mm ضمن سماحية)، وإهمال الخطوط المفردة (أبعاد) والمتقاربة جداً (hatching).
- تمييز الجدران الخارجية عن الداخلية حسب موقعها على الكفاف الخارجي للمبنى، لا بنسبة ثابتة.

### المرحلة C — المحيط الخارجي بالكفاف الحقيقي (تصحيح للنقطة #1)
> ⚠️ Bounding Box و Convex Hull **حلّان خاطئان** للمباني غير المستطيلة (شكل L، بروزات، تجاويف).
- **الإصلاح الصحيح:** تتبّع **الكفاف الخارجي الحقيقي (outer contour polygon)** لخطوط الجدران، وحساب محيطه فعلياً. هذا يعطي المحيط الحقيقي حتى للفلل غير المنتظمة.

### المرحلة D — توصيل الكود (النقطة #4، صحيحة)
1. حذف الكود الوهمي/الميت في `wall_extractor.py` و`arch_extractor.py` (الدوال غير المستدعاة).
2. تمرير `pdf_path` و`internal_page_idx` من `worker.py` إلى `extract_page`.
3. توحيد منطق حقن أرقام OCR والكفاف داخل `step3_extract.py` في مكان إرسال الـ prompts الفعلي.
4. إزالة كل عبارة `100% Precision` من الكود (الأسطر 610، 686، 810، 944، 958 في `step3_extract.py`)، واستبدالها بمستوى ثقة صادق (`high / medium / low`).

### المرحلة E — التحقق الصادق (تصحيح للنقطة #3)
- تشغيل `validation_set/score.py` بعد كل تعديل ومراقبة نسبة `<15%`.
- **توقّع واقعي:** إصلاح محرك الجدران سيحسّن فقط البنود المعتمدة على العمارة (البلاستر، الدهان، الطابوق، المحيط). البنود الإنشائية الفاشلة (`column_concrete=0`، الحديد، `backfill`) **مصدرها مستخرِجات أخرى ولن تُصلَح بهذه الخطة**. لا تَعِد بنجاح الـ validation كاملاً.

---

## 4) ترتيب الأولويات (Execution Order)

1. **A — المقياس** (يحجب كل شيء بعده).
2. **D — توصيل الكود** (حتى يعمل المحرك في كل المسارات + حذف ادعاء 100%).
3. **C — الكفاف الخارجي الحقيقي** للمحيط.
4. **B — أزواج الجدران** للداخلي/الخارجي.
5. **E — التحقق** المستمر بالأرقام قبل أي إعلان جاهزية.

---

## 5) معيار "جاهز للإنتاج" الحقيقي (Definition of Done)

لا يُقال "Production Ready" إلا عند تحقّق كل ما يلي:
- [ ] لا توجد أي عبارة "100% Precision" في الكود.
- [ ] المقياس يُستنتج من مرجع حقيقي، لا يُفترض ثابتاً.
- [ ] المحيط الخارجي محسوب من الكفاف الحقيقي (يعمل على فيلا L-shape).
- [ ] الفيكتور يعمل في `worker.py` و `api/` كليهما.
- [ ] نسبة `<15%` في `score.py` تجاوزت 70% من بنود العمارة.
- [ ] أي ملف بثقة منخفضة يُعرَض للمستخدم بوضوح كـ "تقديري".
- [ ] تنظيف جذر المشروع من ملفات الفحص المؤقتة (`check_*.py`, `test_*.py`, ملفات `.db` الفارغة).

---

## 6) ما هو صحيح فعلاً (الإنصاف)

- الفلسفة الهجينة (AI + Math) اتجاه صحيح.
- تقييد الـ AI بأرقام OCR (`arch_extractor.py:47`) فكرة ممتازة لتقليل الهلوسة.
- "Bullshit Filter" (`step3_extract.py:816-833`) خطوة دفاعية ذكية.

المشكلة ليست في الرؤية، بل في أن التنفيذ تخميني يُسوَّق كمنتج نهائي. أصلِح المحرك بصدق، وستملك منتجاً يستحق فعلاً أن يُدفَع مقابله.
